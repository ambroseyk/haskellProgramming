-- isPalindrome :: (Eq a) => [a] -> Bool
-- isPalindrome l = l == reverse l

-- myAbs :: Integer -> Integer
-- myAbs x = if x < 0 then -x else x

-- f :: (a, b) -> (c, d) -> ((b, d), (a, c))
-- f t p = ((snd t, snd p),(fst t, fst p)) 


-- x = (+)
-- f xs = w `x` 1
--     where w = length xs

 --  anonymous   
f (a, b)= a